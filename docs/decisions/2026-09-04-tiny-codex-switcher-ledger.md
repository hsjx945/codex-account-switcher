# Tiny Codex Switcher decision ledger

| ID | Stable decision | Alternatives rejected | Verification |
| --- | --- | --- | --- |
| D001 | Use S001 at pinned commit as the only inherited codebase | New app, Electron, multi-repo code merge | Git parent and MIT license |
| D002 | Preserve one shared `~/.codex` history and configuration pool | Separate full homes and duplicated history | Store paths and handoff |
| D003 | Manual, confirmed account selection only | Automatic quota rotation | UI flow and non-goals |
| D004 | Official app-server over stdio for OAuth, identity, and usage | Browser cookies, private web APIs, local network service | CodexClient and O001 |
| D005 | File-backed profiles plus AES-GCM rollback backup with Keychain key | Plaintext rollback file, silent best-effort copy | Encryption and permission tests |
| D006 | Persist every transaction phase and recover on startup | Memory-only stages | Interrupted activation test |
| D007 | Preserve a verified committed target on Desktop reopen failure | Roll back a successfully committed account | Committed recovery test |
| D008 | Serialize account mutations and app-server profile operations | Parallel per-profile refresh during switching | Shared AccountOperationGate |
| D009 | Do not touch real `~/.codex` in automated tests | End-to-end mutation of user credentials | Temporary-path fixtures |
| D010 | Do not close the task-hosting ChatGPT app for acceptance | Self-terminating live switch during development | Closeout reports real switch unverified |
| D011 | Treat the user's completed two-account Plus switch as live A/B acceptance | Repeating a credential-changing switch during this milestone | User confirmation on 2026-09-04 |
| D012 | Default to the full sign-in email; store an optional nickname and offer email, nickname, or both as a global display style | Reusing the historical email local-part as the default label | Legacy decode and nickname persistence checks |
| D013 | Sort the active account first only in the presentation layer and use a fixed low-saturation gray-blue background | Reordering the registry or using a vivid accent color | `displayedAccounts` and AccountRow |
| D014 | Use official `account/usage/read` daily buckets for per-account totals; show locally scanned model totals separately for all accounts on this Mac | Falsely attributing rollout models to an account without a reliable account ID | RPC and synthetic JSONL delta checks |
| D015 | Keep 5-hour warmup experimental, disabled by default, once per account per local day, and skip an active server window | Silent always-on pings or automatic account rotation | Legacy settings, history, model selection, and fake-exec checks |
| D016 | Defer Claude subscription switching/quota to a separate provider milestone; consider read-only local Claude statistics first | Mixing unrelated credential and quota semantics into the Codex data path | Research note and public API boundary |
| D017 | Show the active account with both presentation-first ordering, a low-saturation row, and an explicit Current capsule | Relying on color or ordering alone | AccountRow source and build; packaged visual capture remains unavailable |
| D018 | Five-hour reset reminders are opt-in and request macOS authorization only when enabled | Prompting at launch or enabling silently | Legacy settings and fake permission checks |
| D019 | Deduplicate reminders by the server-provided five-hour reset timestamp persisted per profile | Repeated notification on every five-minute refresh | Reset detector and cache migration checks |
| D020 | A notification action may switch directly only when Desktop is absent; while Desktop runs, active status gets a specific warning and every other independently observed status remains unknown and requires confirmation | Treating a separate app-server's idle result as proof that Desktop is idle | Fake thread/list status matrix and switch-disposition checks |
| D021 | Read `account.planType` from official `account/read`, distinguish Plus/Pro/Pro Lite badges, format all account-row timestamps as Chinese Beijing time, and render a 5-hour row only for an exact server-provided 300-minute window | Guessing a plan from quota size, conflating Pro Lite with Pro, hard-hiding all Pro windows, system-locale English dates, or content-driven bar widths | Account identity fixtures, Beijing formatter check, weekly-only window check, and fixed AccountRow bar width |
| D022 | Expand the popover to 400 pt, remove the non-informative initials avatar, label `prolite` as `PRO 5X` and `pro` as `PRO 20X`, and distinguish the active account with a dynamic neutral card plus a thin accent rail, outline, and explicit badge | A cramped 326 pt layout, large colored selection fills, hard-coded gray-blue backgrounds, or color-only state | Packaged 0.5.0 popover screenshot, plan badge tests, and source inspection |
| D023 | Use a 420 pt icon-led account card, place `当前` immediately before a fixed-width plan badge, hide the five-hour row for both `prolite` and `pro`, and source the active account's current-day tokens/model from local session events using Beijing day boundaries | Tiny critical text, decorative active rails, weak active backgrounds, treating a missing server day as zero, or attributing shared local history to inactive accounts | Packaged 0.5.1 screenshot, real local aggregate, plan/token core checks, and scanner fixture |
| D024 | Adopt the approved interactive prototype in the native SwiftUI popover: use one neutral card system, keep the active account presentation-first, show `当前` immediately left of the right-aligned Free/Plus/Team/Pro plan tag, prefer nickname with email fallback, and show only the active account's current-day Token total | Per-account decorative background colors, initials avatars, an account-name display-mode setting, inactive-account Token summaries, or any 5-hour row on Pro 5x/20x | SwiftUI build, preferred-label and plan tests, core checks, packaged app runtime, and real popover inspection |
| D025 | Let lists with up to four accounts take their natural SwiftUI height and give larger lists an explicit 520 pt scroll viewport; represent active identity as checking, confirmed, temporarily unavailable, or mismatched, with retry for the two failure states | A max-height-only `ScrollView` inside `MenuBarExtra`, or one ambiguous `activeIdentityConfirmed` warning that discards whether verification failed or actually mismatched | 0.6.1 debug/release builds, full tests, core checks, source assertions, signed packaged runtime, and the reported collapsed-popover regression |
| D026 | Use fully opaque adaptive surfaces for the popover, account cards, settings sections, and colored status/plan tags; keep a missing identity response silent and show a warning only for a proven saved-account mismatch | Translucent material that lets the desktop bleed through, low-alpha tag fills, or treating a successful `account/read` response with no identity as an account conflict | 0.6.2 build/tests/core checks, safe app-server envelope diagnostic, signed packaged runtime, and user-reported contrast/false-warning regressions |
| D027 | Render all account-card text at primary contrast, expose a labeled per-account remark action, and show the current remark or email directly in the macOS menu bar before the optional percentage | Gray secondary labels/dates, an undiscoverable pencil-only remark action, or a menu-bar percentage with no account identity | 0.6.3 build/tests/core checks, signed packaged runtime, and the user-reported readability/current-account gap |
| D028 | When Token statistics are enabled, show today's server-attributed Token usage on every account card and a combined total below the account list; use unavailable rather than allocating unidentifiable local history | Active-account-only Token display, no combined total, or fabricated per-account allocation from shared local sessions | 0.6.3 build/tests/core checks and packaged runtime |
| D029 | Use the latest common server Token date across saved accounts, label that date explicitly when it is not today, and distinguish loading from a completed no-data result | Treating a missing current-day bucket as an endless calculation, showing zero for an absent day, or summing accounts from different dates | Safe live schema/cache diagnostics, 0.6.4 build/tests/core checks, and packaged runtime |
| D030 | Active: on an empty installation, the first completed browser login atomically becomes the active profile; failed or cancelled additions remove their unregistered profile directory | Leaving the first account unusable, or retaining an unregistered credential after a failed add | First-add core check, AccountStore test, cancellation-path source inspection, and release build |
| D031 | Active: a committed switch journal never rolls back the verified target because a later identity read, Desktop reopen, or journal cleanup is temporarily unavailable | Treating post-commit recovery like a pre-commit transaction | Committed recovery failure core check and SwitchCoordinator source inspection |
| D032 | Active: account removal commits registry state before deleting the saved credential and restores registry membership when physical deletion fails | Deleting the only credential before an accounts.json commit that can fail | Immutable-profile fault injection in core checks |
| D033 | Active: the aggregate Token date is the latest date in the real intersection of every saved account's server buckets; do not scan local session JSONL when no UI consumes that result | Taking the minimum of per-account latest dates, or periodic private-session I/O with no user-visible output | Sparse-bucket intersection tests, source inspection, and core checks |
| D034 | Active: a valid weekly or exact five-hour usage window remains visible when its reset timestamp is absent, with an explicit localized unknown-reset label | Dropping the entire usage window because resetsAt is missing | Missing-reset normalizer tests, release build, and AccountRow source inspection |
| D035 | Active: CI must fail closed when Swift Testing discovers no known tests, handle a zero before-SHA on a repository's first push, and pin all privileged Pages actions to immutable commits | Treating test-bundle compilation as executed tests, invalid first-push diffs, or mutable action tags with private-repository permissions | Test discovery guard, empty-tree diff simulation, pinned-SHA review, and site checks |

Open material questions: incremental rollout indexing, statistics completeness UI, project-to-account bindings, usage alerts, and an optional Claude provider. Public signing, notarization, and release remain separately authorized actions.
